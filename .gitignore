# 1. Ignore EVERYTHING by default
*

# 2. But, "un-ignore" directories so Git can look inside them
!*/

# 3. "Un-ignore" this .gitignore file itself
!.gitignore

# 4. "Un-ignore" all VHDL files (files ending in .vhd)
!*.vhd

# 5. "Un-ignore" Python files
!*.py

# 6. But, RE-IGNORE common Python cache/junk folders
__pycache__/
*.pyc
